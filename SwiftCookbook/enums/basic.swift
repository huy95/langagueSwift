enum Direction {
    case up
    case down
    case left
    case right
}
enum WeekDay { case Monday, Tuesday, Wednesday, Thursday, Friday, 
Saturday, Sunday }

let dir = Direction.up
debugPrint(dir)

let beginWeek:  WeekDay =  WeekDay.Monday
debugPrint(beginWeek)
